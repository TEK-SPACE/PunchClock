@using Kendo.Mvc.UI.Fluent
<div id="kwDialog">
</div>
<div id="loadingDiv">
    <div id="ajax_loader">
    </div>
</div>

@*@(Html.Kendo().Tooltip()
      .For(".regDropdown")
      .ContentHandler("getTooltip")
      .Width(120)
      .Position(TooltipPosition.Top)
      )*@

@if (User.Identity.IsAuthenticated)
{
    @(Html.Kendo()
        .Window()
        .Name("TimeTracker")
        .Width(500)
        //.Height(315)
        .Draggable()
        .Resizable()
        .Title("Time Tracker")
        .Actions(actions => actions.Pin().Refresh().Maximize().Minimize().Close())
        //.Position(x=>x.Left(100).Top(100))
        .LoadContentFrom("PunchWindow","Punch").Events(x=>x.Refresh("convertUtcToLocal").Open("centerPunchWindow"))
        .Animation(true)
        .Modal(true))
}